(byte) @constant

[
  (address)
  (hexadecimal)
  (integer)
] @number

(identifier) @variable

(bad_instruction) @comment.warning

(code_location
  (identifier) @function.call)

(comment) @comment

(instruction) @function

(memory_dump) @string

[
  "<"
  ">"
] @punctuation.special

[
  "+"
  ":"
] @punctuation.delimiter
