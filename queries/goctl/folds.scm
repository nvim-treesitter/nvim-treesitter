; Fold rules for goctl

; Fold types
(typeGroupSpec) @fold
(typeSingleSpec) @fold
(typeStruct) @fold

; Fold services
(serviceSpec) @fold
(serviceBody) @fold

; Fold import groups
(importGroup) @fold
(importSpec) @fold

; Fold info
(infoStatement) @fold

; Fold server
(serviceServerSpec) @fold

; Fold struct
(structType) @fold

