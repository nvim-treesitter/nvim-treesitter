(identifier) @reference

(program (named_node) @scope)
(program (anonymous_node) @scope)

(named_node
  (capture) @definition.var)

(anonymous_node
  (capture) @definition.var)

(grouping
  (capture) @definition.var)
