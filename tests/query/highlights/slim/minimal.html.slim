doctype html
/ <- tag.builtin
/       ^ markup.raw.block
html
/ <- tag.builtin
  head
  / <- tag.builtin
    title A Page
    / <- tag.builtin
    /     ^ markup.raw.block

unknow_tag
/ ^ tag

div class=classes
/ ^ tag.builtin
/   ^ variable.parameter
/        ^ tag.delimiter
/         ^ tag.attribute

section with = "open parameters"
/ <- tag.builtin
/       ^ variable.parameter
/            ^ tag.delimiter
/              ^ string
/               ^ string
  article(with = "enclosed parameters") and content
  / <- tag.builtin
  /      ^ punctuation.bracket
  /       ^ variable.parameter
  /              ^ string
  /               ^ string
  /                                     ^ markup.raw.block

section(with = "wrapped parameters")
/ <- tag.builtin
/      ^ punctuation.bracket
/       ^ variable.parameter
/            ^ tag.delimiter
/              ^ string
/               ^ string
/                                 ^ string
/                                  ^ punctuation.bracket
  article(with = "enclosed parameters") and content
  / <- tag.builtin
  /      ^ punctuation.bracket
  /       ^ variable.parameter
  /            ^ tag.delimiter
  /              ^ string
  /               ^ string
  /                                  ^ string
  /                                   ^ punctuation.bracket
  /                                     ^ markup.raw.block

/ comment
/ ^ spell
/   ^ spell
/      :-)

div#content.is-fixed= t('sentense')
/ ^ tag.builtin
/  ^ keyword.function
/           ^ keyword.type
/                   ^ keyword.directive

span
  b: strong
/ ^ markup.strong
/    ^ markup.strong

span
  i: em
/ ^ markup.italic
/    ^ markup.italic

span
  a
/ ^ markup.link

span
  h1: h2: h3
/ ^ markup.heading.1
/     ^ markup.heading.2
/         ^ markup.heading.3

span
  ul: ol: li: dl: dt: dd
/ ^ markup.list
/     ^ markup.list
/         ^ markup.list
/             ^ markup.list
/                 ^ markup.list
/                     ^ markup.list
